# Canonical site URL (no trailing slash). Used for sitemap.xml, robots.txt, metadata.
# Set this in Vercel for Production (and Preview if you want preview sitemaps to match prod).
NEXT_PUBLIC_SITE_URL=https://gitreview.thomascormican.xyz

# Base URL of the GitReview AI backend API
NEXT_PUBLIC_API_URL=http://localhost:4000
